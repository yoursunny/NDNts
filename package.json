{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npm run bootstrap1 && lerna bootstrap --hoist",
    "bootstrap1": "node mk/update-tsconfig-paths.js",
    "build": "lerna exec -- tsc -d",
    "build2": "npm run build && lerna exec -- tsc --removeComments true",
    "clean": "rm -rf packages/*/lib/",
    "cover": "jest --coverage",
    "lint": "tslint --fix -p .",
    "publish": "npm run build2 && lerna publish --no-push",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.4",
    "jest": "^24.9.0",
    "lerna": "^3.16.4",
    "ts-jest": "^24.0.2",
    "tslint": "^5.20.0",
    "typescript": "~3.6.2"
  }
}
