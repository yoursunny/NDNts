{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec tsc",
    "publish": "lerna exec tsc && lerna publish --no-git-tag-version --no-push"
  },
  "devDependencies": {
    "@types/node": "^12.7.4",
    "lerna": "^3.16.4",
    "ts-lint": "^4.5.1",
    "typescript": "^3.6.2"
  }
}
